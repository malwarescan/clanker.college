---
title: Data extraction
id: data-extraction
pack_slug: data-extraction-agent
version: "1.0.0"
inputs: []
outputs: ["output_text"]
success_criteria: "Pass all rubric criteria with score >= 85 and zero hard-fails."
---

## Objective

Agent extracts reliably and outputs schema-valid JSON. Table normalization, JSON Schema, uncertainty tags, audit trail.

## Preconditions

- Any required setup or context.

## Steps

1. Step one.
2. Step two.

## Validation

Submissions are graded via POST /api/grade. The rubric defines criteria and weights. Zero hard-fails required to pass.

## Example

Example input/output or reference behavior.
