# Copy to .env or .env.local and fill in. Do not commit .env / .env.local.

# Replace host/user/pass with real values from your Postgres service. Do not deploy with literal "host".
DATABASE_URL=postgresql://user:pass@YOUR_DB_HOST:5432/dbname
# Optional. Omit or use real Redis URL; literal "host" will cause ENOTFOUND in production.
REDIS_URL=redis://default:pass@YOUR_REDIS_HOST:6379

# App URL (checkout success/cancel redirects)
NEXT_PUBLIC_APP_URL=https://clanker.college

# Clerk (auth). Dev: use pk_test_/sk_test_. Production: use pk_live_/sk_live_.
# Set NEXT_PUBLIC_CLERK_ENABLED=true only after adding a valid publishable key.
NEXT_PUBLIC_CLERK_ENABLED=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Optional: for webhooks (e.g. user sync). Set in production if using webhooks.
# CLERK_WEBHOOK_SECRET=whsec_…

# Clerk Hosted Pages (production). When set, Sign in → accounts.clanker.college/sign-in, then back to clanker.college/account.
# NEXT_PUBLIC_CLERK_ACCOUNT_PORTAL_URL=https://accounts.clanker.college
# Paths in Clerk Dashboard: Sign in https://accounts.clanker.college/sign-in, After sign in https://clanker.college/account

# Stripe (live). Pack → Stripe mapping: edit packages/db/prisma/stripe-products.json then pnpm db:seed
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Subscription plans (Stripe Price IDs)
STRIPE_PRICE_INDIVIDUAL_MONTHLY=
STRIPE_PRICE_INDIVIDUAL_ANNUAL=

# Optional
SENTRY_DSN=
POSTHOG_KEY=
